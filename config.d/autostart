# Commands to be executed on startup of Sway.

# Sets the window manager name so that JetBrains IDE's work properly.
exec_always /usr/bin/wmname LG3D

# Starts the playerctld daemon to always target the last audio output
# with the media key controls.
exec_always /usr/bin/playerctld daemon

exec_always /usr/bin/mpris-proxy

